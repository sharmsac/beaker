const Discord = require('discord.js');
const embedcolors = require('../../../JSONS/embedcolors.json');

module.exports = {
    name: 'nodeConnect',
    description: 'nodeConnect Event for ErelaClient'
}

module.exports.run = async function(client, node){

    console.log("New node connected");
    
}
